
 {{#if success_msg}}
    <div class="alert alert-success alert-dismissible fade show" role="alert">
     <strong>{{success_msg}}</strong> 
     <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
 {{/if}}

{{#each posts }}
<div class="card">
    <div   class="header">
        <div>
            <h6 class="m-2">{{this.title}}</h6>
            <div  class="user-info">
                <img class="user_profile m-2"  src="{{user.profile}}" alt="profile">
                <div class="m-2">
                     <span>Posted by: <b> <a href="/user/details/{{user._id}}"> {{user.name}} </a></b></span>
                     <br>
                     <span><i>on {{ formatDate createdAt 'dddd, MMMM Do YYYY, h:mm a'}}</i></span>
                </div>
            </div>
        </div>
        {{#ifCond this.user._id  @root.userId }}
        <div  class="btnHolder" >
            <form action="/posts/delete/{{_id}}" method="POST">
                <input type="hidden" value="DELETE" name="_method" />
                <button class="btn" type="submit"> <i class="far fa-trash-alt"></i></button>
           </form>
           <a class="btn" href="/posts/update/{{_id}}"><i class="fas fa-edit"></i></a>
        </div>
        {{/ifCond}}
    </div>
    <hr>
    <div class="body">
        <P id="dot">{{{ getShortPost body}}} </P>
        <p id="more">{{body}}</p>
        <img id="body-img" class="body-img-sm" src="{{image}}" alt="image">
    </div>
</div>
{{else}}
<h4>Create new Post</h4>
{{/each}}